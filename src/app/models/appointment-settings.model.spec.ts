import { AppointmentSettings } from './appointment-settings.model';

describe('AppointmentSettings', () => {
  it('should create an instance', () => {
    expect(new AppointmentSettings()).toBeTruthy();
  });
});
